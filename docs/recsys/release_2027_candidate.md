# Кандидат выпуска сезонов ЕГЭ 2027

Подготовлен 20.09.2026. Ветка `release/exam-seasons-2027`, основание `origin/main` — `de1c99b`. Отдельная рабочая копия: `D:\FS\django-site-release-2027`. Это кандидат, не выполненная выкладка на рабочий сервер.

## Состав

Включены ежегодные версии экзамена, назначения общих задач, сохранение контекста выдачи и старой истории, миграции recsys 0043–0049, административный переход года, отдельное оценивание пары чисел 2027 и исправление CSRF тренировок. В журнале системы показывается сохранённый тип ответа.

Учёт учеников, расписание, их миграции и изменения главной страницы остались в общей ветке `work/local-development-2026-09-13`. Зависимость страницы учителя от нового `student_record` исключена из кандидата; используется существующая в main связь teacher/student. Рабочая папка исходной разработки и её тестовая база не заменены.

## Содержание информатики 2027

- Сети: 13/2026 → 10/2027; исполнитель: 23/2026 → 13/2027. Поиск в редакторе исключён из новой выдачи, сохранён в старом году.
- В новый банк добавляются пять проверенных задач: демо 13, пользовательская рекурсия `k27076`, демо 23, демо 24 и демо 27 с согласованным уточнением центра.
- Старые задачи 27 остаются в 2026. Адаптация части B демо 2026 в выпуск не импортируется.
- В 27/2027 обязательна «Кластеризация», «Аномалии» сохранены как дополнительная тема. Требования 2026 и сам тег не удаляются.
- Шкала перевода во вторичные баллы отключена. Документы имеют статус проекта; банк 23 пока содержит только кратчайший путь, банк 27 — одну задачу.

## Команда переноса

`prepare_informatics_2027_release` использует slug исходного экзамена и проверяет содержимое существующих задач, а не ID из тестовой базы. Создаёт **только черновик**. Существующий чужой сезон или несовпадающую задачу не перезаписывает. Повтор своего импорта не меняет редакторские правки и не возвращает выведенные из выдачи задачи.

В каталог `FILES` положить `demo_23.txt`, `demo_24.txt`, `demo_27.txt` из проверенного комплекта 2027. SHA-256 проверяется до записи. Тексты задач входят в код (`scripts/data`), сами файлы передаются отдельно. Не подменять их одноимёнными файлами другого выпуска без повторной проверки и обновления контрольных сумм.

Пример команд **для заранее выбранного окружения**, где `DB_NAME` — настоящее имя его базы, а `FILES` и `REPORT` — подготовленные пути:

```text
python manage.py prepare_informatics_2027_release --source-exam-slug inf-ege-2026 --files FILES --report REPORT --settings=fractalschool.settings
python manage.py prepare_informatics_2027_release --source-exam-slug inf-ege-2026 --files FILES --expect-database DB_NAME --apply --report REPORT --settings=fractalschool.settings
```

Первая команда откатывает изменения базы и не пишет media. Как обычно в PostgreSQL, использованные значения последовательностей ID не возвращаются; это не создаёт записи задач. Вторая требует совпадения `--expect-database` с выбранной настройками базой. Новые вложения при ошибке импорта удаляются; существующие не заменяются. Отчёт содержит `ready_for_review` и список оставшихся замечаний. Публикация — отдельное действие в админке после проверки требований.

Ранее созданные `scripts/*sandbox*`, `complete_informatics_2027.py` и `add_informatics_2027_demo13.py` — сценарии исходного локального эксперимента. Для рабочего переноса использовать **management-команду выше**. Не восстанавливать тестовую базу поверх рабочей.

## Проверки кандидата

- Проверки Django и отсутствие недостающих миграций проходят.
- В полном прогоне 171 тест: все 143 теста recsys проходят, включая 6 новых проверок команды переноса и CSRF-проверку запуска тренировок.
- Полный набор: 5 failures и 10 errors. Чистый `origin/main` (`de1c99b`, 114 тестов) воспроизводит тот же список: 7 ошибок из-за отсутствующего Node, 3 теста courses создают Task без slug, несоответствие JSON-ожидания в accounts, 3 проверки даты акции 2025/2026, сохранение выбранных предметов заявки. Новых падений в кандидате не обнаружено; полный набор не считается прошедшим.
- Добавлен отдельный GitHub Actions workflow `Exam season checks` с Python 3.11 и PostgreSQL 17. Он выполняет только профильные проверки, не развёртывает сайт. Образец подключения сервиса: [GitHub Docs](https://docs.github.com/en/actions/tutorials/use-containerized-services/create-postgresql-service-containers); настройка Python: [actions/setup-python](https://github.com/actions/setup-python).

Репетиция выполнена в `exam_release_rehearsal_20260920`, отдельной копии имевшейся локальной `exam_year_restored`. Это **не свежая копия сегодняшнего прода**. На ней:

1. Применена миграция 0049 (0043–0048 были в исходной локальной копии).
2. Предварительная проверка вернула готовность к методической проверке без сохранения сезона.
3. Импорт сохранил черновик; повтор не создал дубли.
4. Аккаунты, связи учителя с учениками, ответы, старые варианты, тренировки и агрегаты сравнены с исходной копией по контрольным суммам — совпадают.
5. Пробная публикация, ученик и ответы созданы внутри откатываемой транзакции: вариант содержит 27 различных задач, максимум 29, за переставленную пару начисляется 1 балл.
6. Запуск тренировки с `enforce_csrf_checks=True` возвращает 201, три новых файла — HTTP 200. После проверки остался только черновик, проверочные ответы не сохранены.

Отчёты и логи находятся в `.local/release-2027/`, исключены из Git. Исходная пользовательская `exam_year_sandbox` не менялась при сборке релиза.

## Что требуется перед выкладкой

1. Состав подтверждён владельцем 20.09.2026: выпускаем только сезоны; учёт учеников и расписание — позже.
2. По решению владельца используем имеющуюся тестовую копию для репетиции, новый дамп для неё не запрашиваем. Репетиция выше проверила 0049 и импорт; полный путь 0043–0049 и backfill требует отдельной проверки на копии до сезонных миграций. Перед изменением рабочей БД на сервере сделать резервную копию для восстановления.
3. Действующий сервер установлен: Railway, проект `eloquent-creation`, окружение `production`, сервис `web`, домены `fractalschool.ru` и `www.fractalschool.ru`. Источник — `EPMAKOBB/django-site`, ветка `main`, ожидание GitHub checks выключено. Владелец выполнил `railway login`, SSH проверен. Старый FTP workflow удалён; он не описывал действующий прод.
4. Разобрать известные сбои общего набора; отдельно проверить реальные формы заявок и экран учителя. Зафиксировать решение по ним перед выпуском.
5. Выполнить браузерную приёмку на копии с media: старый незавершённый вариант, история, новый ответ, запуск/завершение тренировок 2026/2027, выбор сезона существующим учеником.
6. Согласовать окно первого обновления с паузой записи ответов и фоновых задач. Выполнить резервную копию базы и media, проверить восстановление.

## Порядок выкладки после репетиции

Устаревший `.github/workflows/main.yml` удалён. Оставшийся workflow выполняет проверки, а не выкладку. **Слияние в main запускает автодеплой Railway.** Не сливать релиз до подготовки БД и согласованного окна первого обновления.

### Проверенные настройки Railway (20.09.2026)

- Project ID: `dc4edee0-580a-4685-8900-56605bb196e2`.
- Environment ID: `4514b284-4866-4c8a-a609-6edc09caf5b3`.
- Service ID: `76cd0305-0195-4797-a24f-ea0c4dc692f0`.
- Builder: Nixpacks. Start command: `sh -c "gunicorn fractalschool.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 2 --timeout 120 --access-logfile - --error-logfile -"`.
- Pre-deploy command не настроена. В Procfile release выполняет только `collectstatic`; ни один из этих механизмов сейчас не выполняет `migrate` или backfill.
- Подключён volume `/app/media`; заданы переменные S3. Фактическое хранилище файлов и используемую БД определить по рабочим настройкам без вывода секретов.
- В production две службы PostgreSQL; нельзя выбирать базу по названию. Использовать подключение действующего web и проверить соответствие перед резервным копированием.
- Проект `testing-1` использует ветку `feature/recsys-v2`, это другой сервис; не менять его при выпуске.

После `railway login` безопасная первая команда для проверки миграций:

```powershell
railway ssh --project dc4edee0-580a-4685-8900-56605bb196e2 --environment 4514b284-4866-4c8a-a609-6edc09caf5b3 --service 76cd0305-0195-4797-a24f-ea0c4dc692f0 python manage.py showmigrations recsys --settings=fractalschool.settings
```

Первый backfill требует остановки старых писателей. Нельзя просто добавить его в pre-deploy и считать паузу обеспеченной. Кроме того, [Railway pre-deploy](https://docs.railway.com/deployments/pre-deploy-command) не предоставляет подключённый volume; файловые отчёты и импорт media нужно выполнять в подходящем постоянном хранилище. Настройки Railway пока не изменены.

### Репетиция на свежей копии рабочего сервера

После входа CLI резервная копия получена самостоятельно, без передачи секретов в чат. В рабочем приложении применены миграции по 0042. PostgreSQL — 18.6; использованы локальные инструменты версии 18. По `pg_control_system()` подтверждено, что публичное подключение и работающее приложение используют одну базу — службу `Postgres-bsr9`. Фактическое файловое хранилище — FileSystemStorage, `/app/media`.

Локальные артефакты в `.local/release-2027/` (не включены в Git):

- `production-20260920T180140Z.dump`: 9 952 423 байта; SHA-256 `825ee4b045e6409c83924ee9b0e3c77da9c8f51a4a2ee9a6b8b45ea5b6c852e6`.
- `production-media-20260920.tar.gz`: 126 266 018 байт; SHA-256 `7a197d8a29a6d5ec01ce7a09f05949ca1a76559b19aa89c43c2f15736f6c8662`. Все 136 файлов прочитаны из архива и восстановлены локально.
- Копия восстановлена в отдельный PostgreSQL 18, `127.0.0.1:55439`, БД `exam_release_live_20260920`; неизменная исходная копия — `exam_release_live_before`.
- Все миграции 0043–0049 прошли. Backfill: 1 048 назначений, 2 761 ответ, 298 вариантов, 1 014 тренировок; конфликтов нет. Повторный запуск не вносит повторных изменений. 11 старых задач без годового типа остаются без назначения и не исправляются предположениями.
- Сравнены все прежние столбцы 11 таблиц истории, аккаунтов и освоения, за исключением намеренно дополненных снимков выдачи. Ответы, оценки, время и показатели освоения сохранены.
- Импорт 2027 прошёл без ошибок и оставил черновик. Пробная публикация с откатом: 27 разных задач, максимум 29 баллов, переставленная пара типа 27 — 1 балл; запуск тренировки с CSRF — 201; новые файлы — 200.
- HTTP-проверки Django Client с откатом: кабинет существующего ученика, старый незавершённый вариант, тренировка 2026, ученики преподавателя и системный журнал сотрудника — 200. Это не визуальная проверка браузером.
- Отчёты: `live-backfill-applied.json`, `live-backfill-repeat.json`, `live-history-comparison.json`, `live-import.json`, `live-rehearsal-check.json`, `live-legacy-pages.json`.

### Следующая операция на проде

Согласовать время паузы сайта (ориентир 15–20 минут, длительность зависит от переноса и сборки). На это время остановить только deployment `web` через `railway down --service 76cd0305-0195-4797-a24f-ea0c4dc692f0 --environment 4514b284-4866-4c8a-a609-6edc09caf5b3 --yes` из уже связанной релизной рабочей копии. PostgreSQL и volume не удалять. До остановки сверить отсутствие новых коммитов/развёртываний и проверить готовность PR.

После прекращения запросов сделать финальный дамп. Выполнить миграции и backfill проверенным релизным кодом через подтверждённое публичное подключение, задавая рабочие переменные только в памяти процесса и сохраняя отчёты локально. Не использовать sandbox settings. Проверить отсутствие конфликтов и сохранность оценок. Затем слить PR #247, дождаться успешного автодеплоя нового кода Railway и проверить старые страницы. Доставить три файла демоверсии в служебный каталог нового контейнера, импортировать черновик 2027 штатной командой и проверить его перед публикацией. Не запускать старый код после миграций; при ошибке сохранить данные и исправить релиз.

1. Остановить запись и старые процессы, сохранить контрольные количества/примеры истории.
2. Доставить проверенный код; выполнить миграции с рабочими настройками.
3. Выполнить `audit_exam_context`, предварительный `backfill_exam_context`, затем `backfill_exam_context --apply` и повторный аудит. Сопоставить результаты с копией до обновления. [Подробный порядок и ограничения восстановления](exam_year_transition_release.md).
4. Выполнить `collectstatic --noinput` в окружении сервера. Перезапустить только новые процессы и проверить старый сезон/приём ответов.
5. Подготовить черновик 2027 management-командой; проверить отчёт, новые вложения и требования. Опубликовать через админку; 2026 оставить активным. Выбор существующих учеников не переносить принудительно.
6. Возобновить запись и фоновые задачи, повторить аудит и проверить журнал ошибок.

После начала приёма новых ответов нельзя просто вернуть старый дамп или старый код, не понимающий новые контексты. При сбое остановить новые записи, сохранить принятые данные и исправлять текущий выпуск. Конкретные команды остановки/перезапуска будут добавлены после уточнения окружения действующего сервера.
