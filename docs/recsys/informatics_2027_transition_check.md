# Проверка перехода информатики 2026 → 2027

## Текущее состояние на 20.09.2026

**Последующее решение пользователя:** старые задачи типа 27 оставляем в 2026, новый год наполняем новыми. Адаптация #1180 выведена из выдачи 2027 (назначение `retired`, без удаления истории). В 2027 теперь **одна задача типа 27 — #1181**, адаптация демоверсии 2027 с согласованным уточнением центра. Все десять исходных задач остаются в 2026. Для темы «Аномалии» в новом году снова требуется новая задача; прежние требования не удалялись ради показателя заполненности. Фикстура #1180 помечена `excluded`, скрипт повторного наполнения её пропускает. Ниже сохранён отчёт первоначальной публикации с двумя задачами; архив `completed-20260920.dump` также соответствует состоянию до этого решения.

**ЕГЭ 2027 (ID 11) опубликован только в локальной `exam_year_sandbox` и рекомендован новым пользователям. ЕГЭ 2026 остаётся активным; выбор экзамена у существующих учеников не менялся.**

| Тип 2027 | Сделано |
| --- | --- |
| 10 и 13 | Сохранены соответствия 13/2026 → 10/2027 и 23/2026 → 13/2027; задача #1176 закрывает наличие материала по «Замене цифр». |
| 16 | Пользовательская задача #1177 `k27076` проверена снизу вверх без глубокой рекурсии, ответ 998154. Опубликована, доступна в обоих сезонах без копирования задачи. |
| 23 | Задача #1178 из демоверсии 2027, тема «Кратчайший путь в ориентированном графе», файл `demo_23.txt`, ответ 10971. |
| 24 | Задача #1179 из демоверсии 2027, «Арифметическое выражение», файл `demo_24.txt`, ответ 154; проверен двумя алгоритмами. |
| 27 | #1180 — самостоятельная часть B демоверсии 2026, адаптированная к одной паре чисел, с аномалиями; ответ 142058 25299. #1181 — адаптация демоверсии 2027, ответ 539936 100704. Обе имеют вложения и явное описание происхождения. |

В проекте демоверсии 2027 обнаружена неоднозначность: кластеры имеют 500, 600, 700 и 800 частиц, поэтому минимум суммы абсолютных разностей достигается в двух центральных частицах. Нижний центр последнего кластера даёт 100666, верхний — 100704. Пользователь согласовал уточнение «при равенстве выбрать частицу с большей энергией»; оно явно включено в условие #1181. Ошибка исходного документа не скрыта.

Тип 27 использует одну строку из двух целых чисел. Десять исходных задач с четырьмя числами сохранены в 2026, их назначения в 2027 выведены из выдачи. Для соответствия 27 → 27 задано `partial`, пустой `task_ids`, явный флаг `allow_empty_source_pool` и объяснение причины. Флаг не разрешён для split/merge или без примечания.

Для типов 26 и 27 нового сезона введено отдельное правило `ege_pairs_2027`: два верных числа — 2 балла; одно на верном месте или два переставленных — 1 балл. Старые правила `partial_pairs` и `partial_rows` не менялись. Миграция `0049_annual_empty_pool_and_pair_grading` применена только к локальной тестовой базе.

Проверено: 27 различных задач в варианте, максимум 29 первичных баллов, ответы и тренировки типов 10, 13, 16, 23, 24, 26, 27; страницы экзамена и админки возвращают HTTP 200. Проверочные ученики, работы и ответы откатились. Контрольные суммы старых ответов, работ, тренировок, агрегатов и банка 2026 совпали до и после публикации. **136 тестов recsys прошли на PostgreSQL**, Django check и проверка отсутствия недостающих миграций прошли. Это серверная проверка, не полная браузерная приёмка.

Ограничения: документы ФИПИ пока имеют статус проекта; банк нового типа 23 содержит одну разновидность. Перевод первичных баллов во вторичные оставлен отключённым. В локальной копии отсутствует **251 старое вложение** из доступного банка 2027 — нужна копия media. Все четыре новых вложения на месте. Публикация сезона не исправляет отсутствующие старые файлы.

Запуск: `.\start-local.cmd`; затем `http://127.0.0.1:8001/exams/inf-ege-2027/`. Создавать ещё одну версию 2027 не нужно. Для изменения уже зафиксированных требований используйте новую редакцию через админку.

Код и инструкции сохранены локально, эти изменения ещё не отправлены на GitHub. Данные и media через Git не переносятся. В `.local/exam-sandbox/transition-2027/` сохранены отчёты `content-completion.json`, `publication-completion.json`, журнал `completion-tests.log`, резервная копия перед работой `before-completion-20260920.dump`; итоговые файлы для переноса — `completed-20260920.dump` и `completed-media-20260920.zip` (содержит только имеющиеся локальные media).

Скрипты воспроизведения на исходном черновике, **не для повторной подготовки уже опубликованного сезона**:

```powershell
.\.venv\Scripts\python.exe -X utf8 scripts/complete_informatics_2027.py --files D:/FS/exam_docs/inf_11_2027/Файлы --files-2026 D:/FS/exam_docs/inf_11_2026/Доп.файлы_ИНФ-11_2026 --apply
.\.venv\Scripts\python.exe -X utf8 scripts/publish_informatics_2027_sandbox.py --apply
```

Первый скрипт содержит привязку к проверенной пользовательской задаче #1177. Для другой базы её нужно отдельно сопоставить и проверить. Без `--apply` изменения базы откатываются. Решения задач находятся в `scripts/verify_informatics_2027_content.py`; тексты — в `scripts/data/inf_ege_2027_completion.json`.

## История первоначальной проверки 19.09.2026

Дата: 19 сентября 2026 года. Сценарий задан пользователем. Изменена только локальная база `exam_year_sandbox` на `127.0.0.1:55438`. Рабочий сервер не обновлялся.

**Результат: существующий черновик ЕГЭ 2027 подготовлен, история сохранена. Публикация пока блокируется неполным банком и непроверенными требованиями.**

**Обновление 20.09.2026:** в тип 13 добавлена задача #1176 из проекта демоверсии ФИПИ 2027, задание 13, с тегом «Замена цифр». Полное текстовое условие сохранено в Markdown, источник указан как «ДЕМО 2027 Инф». Ответ **16** сверён с таблицей ответов на странице 27, рекурсивным подсчётом и динамическим программированием. Проверены преобразование ввода формы и оценивание правильного/неправильного ответа. Повторный импорт не создаёт дубль; старые ответы и агрегаты знаний не изменены. Тег обеспечен одной задачей в банке, но освоение ученикам не начислялось. В черновике типа 13 теперь 14 назначений. Замечание о нехватке задачи по этому тегу устранено; остальные ограничения публикации остаются. Разделы ниже описывают исходную проверку от 19.09.2026.

Повторение добавления в локальной копии: `.\.venv\Scripts\python.exe -X utf8 scripts/add_informatics_2027_demo13.py`. Исходное содержимое для воспроизведения хранится в `scripts/data/inf_ege_2027_demo13.json`. Само добавление данных не переносится через Git: на другом устройстве нужен дамп обновлённой базы либо повторный запуск скрипта после подготовки сезона.

## Подготовленная конфигурация

Использован исходный экзамен `inf-ege-2026`, ID 1, и уже существовавший черновик `inf-ege-2027`, ID 11. Новый дублирующий экзамен не создавался.

| Было в 2026 | Стало в черновике 2027 | Задачи и требования |
| --- | --- | --- |
| Тип 10: поиск символов в текстовом редакторе | Исключён | Все 55 исходных задач и старые ответы остаются в 2026. Их черновые назначения в 2027 удалены. |
| Тип 13: IP-адресация компьютерных сетей | Тип 10, ID 90 | Те же 13 задач и 4 обязательных тега. Связь `equivalent`. |
| Тип 23: ветвление и перебор вариантов | Тип 13, ID 100 | Те же 13 задач и 4 обязательных тега, плюс новый тег «Замена цифр». Связь `partial` с явными ID прежних задач. |
| Отсутствовал | Тип 23, ID 87: длина пути в ориентированном графе | Новая область без переноса старых ответов. Пока 0 задач и 0 обязательных тегов; разновидности не выдумывались. Связь `added`. |

Номера типов и соответствующие позиции структуры варианта переставлены согласованно. Временно освобождались уникальные названия, slug и позиции внутри одной транзакции. Неиспользованная запись чернового типа 10 переиспользована для нового типа 23 после удаления её невыданных назначений. **Исходный тип 10/2026 не переименован и не удалён.** В черновике остаётся 27 типов.

В базе старый тип 23 называется «Ветвление и перебор вариантов». В предоставленной демоверсии 2027 показан исполнитель, преобразующий число, в том числе переставляющий цифры. Поэтому описание нового типа 13 уточнено как «Анализ программ исполнителя: преобразование чисел». Это не перенос задач строкового редактора из другой позиции банка.

Тег «Замена цифр» добавлен только в требования нового типа 13. Он не присвоен автоматически старым задачам. Содержимое задач, их исходные типы, теги и ответы не изменены.

## Документы и границы проверки

Использованы локальные файлы из `D:\FS\exam_docs\inf_11_2027`:

- `ИНФ-11 ЕГЭ 2027 СПЕЦ.pdf`: раздел 11, печатная страница 13. Он подтверждает замену тематик 10, 13 и 23. Тема 23 шире одного примера: предусмотрены оптимальные пути и количество путей в ориентированном ациклическом графе.
- `ИНФ-11 ЕГЭ 2027 ДЕМО.pdf`: печатная страница 15, задание 13, и страница 20, задание 23. В задании 23 показана целая часть длины кратчайшего пути во взвешенном ориентированном ациклическом графе.

Документы помечены как **проект от 21.08.2026**. Релевантные страницы прочитаны и просмотрены после рендеринга. Новые разновидности задач по одному примеру демоверсии не утверждались.

В спецификации также изменён формат ответа типа 27: одна строка из двух чисел вместо двух строк по два числа. **По отдельному ответу пользователя тип 27 в этом сценарии не менялся.** Его схема ответа, оценивание, описание и требования в черновике совпадают с исходными; изменение записано как отдельная предстоящая работа. Эта проверка не подтверждает методическую готовность всего экзамена 2027.

## Что проверено

1. **Сохранность копии базы.** До и после подготовки сравнены количества и SHA-256 содержимого строк: весь банк Task, Attempt, работы и ответы вариантов, тренировочные сессии и шаги, снимки прогресса, агрегаты знаний, исходные типы, назначения, теги, структура и шкала. Различий в этих данных нет.
2. **Повторный запуск.** Подготовка применена повторно: повторных задач, ответов, типов и тегов не возникло. Скрипт сохраняет отметку сценария и не переписывает уже подготовленный черновик.
3. **Совместимый опыт на данных 22 учеников.** В транзакции с последующим откатом проверена проекция для будущих активных назначений. Прогресс старого типа 13 совпадает с новым типом 10. Четыре прежние темы исполнителя сохраняют свидетельства, пятая тема «Замена цифр» остаётся нулевой. При прежней формуле среднего новый показатель исполнителя составляет 4/5 старого; это не копирование старого TypeMastery. У нового типа 23 нет прежнего опыта и покрытие равно нулю. Фактических Attempt в 2027 по-прежнему 0.
4. **Страницы приложения.** Django test client под локальным администратором получил HTTP 200 для экзаменов 2026/2027, карточки и проверки черновика, карточек типов 10/13/23. Попытка публикации через административный POST возвращает замечания и оставляет экзамен черновиком. Проверочные изменения откатились. Это проверка серверного рендеринга; полная интерактивная проверка браузером не заявляется.
5. **Автоматический сценарий на PostgreSQL.** Добавлены проверки перенумерации, защиты старых фактов, повторной подготовки, отказа переписывать чужие правки черновика, нулевого прогресса нового типа и запрета публикации неполного банка. На искусственном дополненном банке проверяются публикация, сборка варианта без повторов, новый ответ под новым типом, сохранность старой работы и ответы старой/новой тренировки под своими типами. Искусственные задачи существуют только в тестовой базе Django и не добавлены в пользовательский банк.

Сохранённые количества: **1059 Task, 2664 Attempt, 292 VariantAttempt, 12456 VariantTaskAttempt, 270 тренировочных сессий, 1014 шагов, 55 снимков прогресса**. Исходные 993 назначения информатики ЕГЭ 2026 сохранены. Сохранились также 572 TypeMastery, 1208 TagMastery и 173 SkillMastery.

Итоговый прогон: **132 теста recsys прошли**, включая 6 новых проверок этого сценария. Django check проходит; изменений моделей без миграций нет.

Старый исторический контекст по-прежнему имеет происхождение `inferred`: проверка доказала его сохранность при переходе, но не превратила восстановленную ранее классификацию в достоверно записанную при первоначальной выдаче.

## Исправленная ошибка

`build_type_progress_map()` ранее возвращал `coverage_ratio = 1` для типа без обязательных тегов. Новый пустой тип 23 тем самым мог выглядеть полностью покрытым по этому показателю. Теперь при отсутствии требований покрытие равно 0. Регрессионный тест проверяет это вместе с нулевым прежним опытом. Сохранённые архивные снимки не пересчитывались.

## Что мешает публикации

Текущий отчёт проверки содержит:

- Нет опубликованных задач для новой темы «Замена цифр» в типе 13.
- Нет утверждённых учебных тем и банка нового типа 23; полный вариант из различных задач собрать нельзя.
- Есть прежние пробелы перенесённого банка: «Рекурсия большой глубины» в типе 16 и «Арифметическое выражение» в типе 24. Эти области в данном сценарии не менялись.
- Не подтверждена полная методическая проверка требований и шкалы. Перенесённая шкала 2027 остаётся выключенной.

Перед публикацией также нужно отдельно выполнить согласованную работу по типу 27. До заполнения нового содержания черновик доступен для проверки в админке, но недоступен для обычных новых тренировок. Исходный сезон 2026 остаётся активным.

Открыть результат на запущенном тестовом сайте: `http://127.0.0.1:8001/admin/recsys/examversion/11/change/`. Проверка: `http://127.0.0.1:8001/admin/recsys/examversion/11/review-year/`.

## Воспроизведение

Скрипт `scripts/prepare_informatics_2027.py` разрешает запись только в локальную `exam_year_sandbox` с указанными выше адресом и портом. Он использует сервис перехода года, сохраняет старые данные, останавливается при обнаружении чужих изменений исходного черновика и никогда не публикует экзамен.

```powershell
# Предварительная проверка: вся подготовка откатывается.
.\.venv\Scripts\python.exe -X utf8 scripts/prepare_informatics_2027.py

# Сохранение подготовленного черновика; в текущей копии уже выполнено.
.\.venv\Scripts\python.exe -X utf8 scripts/prepare_informatics_2027.py --apply

# Профильные проверки с реальными миграциями PostgreSQL.
.\.venv\Scripts\python.exe manage.py test apps.recsys --settings=fractalschool.postgres_test_settings
```

Локальные артефакты в `.local/exam-sandbox/transition-2027/`: резервная копия `before-20260919.dump`, отчёты `applied.json`, `repeated.json`, `copy-verification.json`, журнал `recsys-tests.log` и извлечённые страницы документов. Они не входят в Git и не отправлялись на GitHub. Новый код, тесты и этот отчёт пока сохранены локально в рабочей ветке.
